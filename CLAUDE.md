# CLAUDE.md

هذا الملف موجّه لـ Claude Code (AI Agent) — يشرح طبيعة المشروع، القواعد الحاكمة، والطريقة الصحيحة للعمل عليه.

## عن المشروع

**الاسم:** yafa-insurance-system
**الوصف:** نظام إدارة داخلي (Laravel) لإدارة التغطية التأمينية الطبية التي توفّرها مؤسسة يافا لموظفيها وعائلاتهم. كل موظف إله حصة **زيارتين طبيتين شهرياً** (تشمله هو + زوجاته/زوجه + أبناءه + والديه)، بتُستهلك عبر تسجيل موظف الاستقبال للزيارات، مع خصم يُطبَّق حسب القسم الطبي (عيادات/مختبر/صيدلية/أشعة).

**الستاك:**
- Backend: Laravel + MySQL
- Frontend: Blade + Tailwind CSS (+ Alpine.js/jQuery عند الحاجة)
- لا يوجد SPA منفصل — Laravel تقليدي (Blade-driven)

## المرجع الرسمي — اقرأ هذا أولاً

كل تفاصيل التحليل موجودة بمجلد `/docs/reference` (أو المسار اللي حطيناه فيه)، بالترتيب التالي:

1. `README.md` — نقطة الدخول وملخص التوجيه
2. `00-overview.md` — نطاق المشروع، الأهداف، المستخدمون
3. `01-entities.md` — **الكيانات كاملة بالحقول والأنواع والعلاقات — هذا المرجع الأساسي لأي Migration**
4. `02-roles-permissions.md` — الأدوار (Admin/Receptionist) ومصفوفة الصلاحيات
5. `03-business-rules.md` — قواعد العمل الحاكمة (إلزامية، لا تُخالف)
6. `04-workflows.md` — تدفقات العمل التفصيلية
7. `05-use-cases.md` — حالات الاستخدام لكل دور

**لا تبدأ بكتابة أي كود (Migration, Model, Controller) قبل قراءة `01-entities.md` و `03-business-rules.md` كاملين.** هذول الملفين هم مصدر الحقيقة الوحيد لبنية البيانات ومنطق العمل.

## القواعد الحرجة (لا تُخالَف تحت أي ظرف)

هاي القواعد اتفق عليها صاحب المشروع بشكل صريح بعد جلسة تحليل كاملة — أي كود يخالفها يُعتبر خطأ:

1. **وحدة الزيارة = مريض واحد + يوم واحد.** نفس المريض بنفس اليوم = تعديل على الزيارة الموجودة، مش إنشاء زيارة جديدة. حتى لو موظف استقبال مختلف حاول يسجّل، لازم يوجَّه للزيارة الموجودة.
2. **الرصيد الشهري (2 زيارة) على مستوى الموظف ككل** — الموظف + كل تابعيه يستهلكون من نفس الرصيد. مريض تاني (تابع مختلف) بنفس اليوم = زيارة منفصلة تستهلك من نفس الرصيد.
3. **الاستهلاك من الرصيد يحصل مرة وحدة فقط عند إنشاء الزيارة** — إضافة قسم طبي إضافي على زيارة موجودة **لا** تستهلك زيارة إضافية.
4. **الرصيد لا يتراكم شهرياً (No Rollover)** — يُحسب مباشرة (on-the-fly) بعدّ `visits` للموظف وتابعيه ضمن الشهر الحالي. **لا يوجد جدول منفصل للرصيد** — تجنّباً لتكرار مصدر الحقيقة (Single Source of Truth)، `visits` هو المرجع الوحيد.
5. **الخصم ثابت على مستوى القسم الطبي، وليس لكل موظف.** يُنسخ (snapshot) وقت إضافة القسم للزيارة في `visit_departments.applied_discount_percentage` — تغيير الخصم لاحقاً من الإعدادات **لا يؤثر** على زيارات سابقة.
6. **المبالغ المالية اختيارية دائماً** — لا تُشترط لإتمام أي زيارة.
7. **رقم الهوية (9 أرقام) فريد عالمياً** عبر كل الأشخاص بالنظام (موظفين + تابعين)، ويُفحص فوراً عند أي إدخال (من الأدمن أو من الاستبيان).
8. **التابعون (Dependents) كيان موحّد بجدول واحد** (`dependents` مع `type` enum). المريض بالزيارة يُشار له بعمودين على `visits`: `patient_employee_id` و `patient_dependent_id` — بالضبط واحد منهم معبّى، والآخر null دائماً.
9. **الهيكل التنظيمي جدول شجري واحد self-referencing** (`organization_units`)، وليس 3 جداول منفصلة.
10. **دورين فقط:** `admin` و `receptionist`. لا صلاحيات جزئية إضافية داخل نفس الدور في هذه المرحلة.
11. **لا يوجد أي Reporting في هذه المرحلة** — لا تبني شاشات تقارير أو تفترض متطلبات تقارير غير مذكورة صراحة بالوثيقة. البنية يجب أن تسمح بإضافتها لاحقاً، بس مش الآن.
12. **كل عملية جوهرية تُسجَّل بـ `activity_logs`** (إنشاء/تعديل زيارة، موافقة/رفض استبيان، إضافة موظف).

## أسلوب العمل المطلوب

- لا تفترض أي متطلب غير موجود بالوثيقة — إذا في غموض أو نقص، اسأل بدل ما تفترض.
- التزم بأسماء الحقول والجداول المذكورة بـ `01-entities.md` حرفياً (snake_case، بالإنجليزية).
- عند كتابة Migrations، حافظ على الـ Unique constraints المذكورة صراحة (خاصة: `national_id` على مستوى النظام، و`(patient_employee_id, visit_date)` و`(patient_dependent_id, visit_date)` على الزيارات).
- الصلاحيات تُبنى عبر Laravel Gates/Policies حسب `02-roles-permissions.md`.
- الـ Workflows بـ `04-workflows.md` هي المرجع لمنطق الـ Controllers/Services — اتبع نفس التسلسل المذكور فيها بالضبط (خاصة تسلسل الفحوصات: تكرار الزيارة → الرصيد → الإنشاء).

## خارج النطاق حالياً

- أي شاشة تقارير (Reporting)
- أي تكامل مالي/محاسبي خارجي
- تفاصيل فرعية داخل القسم الطبي (نوع الدواء/الفحص) — فقط مبلغ إجمالي قبل/بعد لكل قسم