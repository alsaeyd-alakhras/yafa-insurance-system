# CLAUDE.md

هذا الملف موجّه لـ Claude Code (AI Agent) — يشرح طبيعة المشروع، القواعد الحاكمة، والطريقة الصحيحة للعمل عليه.

## عن المشروع

**الاسم:** yafa-insurance-system
**الوصف:** نظام إدارة داخلي (Laravel) لإدارة التغطية التأمينية الطبية التي توفّرها مؤسسة يافا لموظفيها وعائلاتهم. كل موظف إله حصة **زيارتين طبيتين شهرياً** (تشمله هو + زوجاته/زوجه + أبناءه + والديه)، بتُستهلك عبر تسجيل موظف الاستقبال للزيارات، مع خصم يُطبَّق حسب القسم الطبي (عيادات/مختبر/صيدلية/أشعة).

**الستاك:**
- Backend: Laravel + MySQL + Fortify (مصادقة، custom guard بـ username)
- Frontend: Blade + **Bootstrap 4/5** (admin template جاهز، RTL عربي، خط Almarai) + jQuery + DataTables (`yajra/laravel-datatables-oracle`) + select2 + toastr — **ليس Tailwind فعلياً بالكود الحالي رغم أي وصف سابق**، اتبع نمط Bootstrap الموجود بكل صفحة جديدة
- لا يوجد SPA منفصل — Laravel تقليدي (Blade-driven)، نمط DataTable موحّد (راجع `docs/plans/phase-5-entities.md` للتفصيل الكامل ومرجع النسخ)

## المرجع الرسمي — اقرأ هذا أولاً

كل تفاصيل التحليل موجودة بمجلد `/docs/reference` (أو المسار اللي حطيناه فيه)، بالترتيب التالي:

1. `README.md` — نقطة الدخول وملخص التوجيه
2. `00-overview.md` — نطاق المشروع، الأهداف، المستخدمون
3. `01-entities.md` — **الكيانات كاملة بالحقول والأنواع والعلاقات — هذا المرجع الأساسي لأي Migration**
4. `02-roles-permissions.md` — الأدوار (Admin/Receptionist) ومصفوفة الصلاحيات
5. `03-business-rules.md` — قواعد العمل الحاكمة (إلزامية، لا تُخالف)
6. `04-workflows.md` — تدفقات العمل التفصيلية
7. `05-use-cases.md` — حالات الاستخدام لكل دور

**لا تبدأ بكتابة أي كود (Migration, Model, Controller) قبل قراءة `01-entities.md` و `03-business-rules.md` كاملين.** هذول الملفين هم مصدر الحقيقة الوحيد لبنية البيانات ومنطق العمل.

## خطة التنفيذ الفعلية — `/docs/plans`

بعد وثيقة التحليل (`docs/reference`)، خطة التنفيذ التقني الكاملة (كيف نبني فوق الكود الحالي تحديداً) موجودة بـ `docs/plans/`:

- `docs/plans/README.md` — نقطة الدخول، ترتيب المراحل، القرارات المحسومة
- `docs/plans/phase-1-cleanup.md` → `phase-6-workflows.md` — تفصيل كل مرحلة تنفيذية
- `docs/plans/open-questions.md` — كل الأسئلة المفتوحة أثناء التخطيط وقراراتها النهائية (محسومة بالكامل، ارجع لها عند أي التباس بخصوص سلوك الحذف/الصلاحيات)
- `docs/plans/TASKS.md` — **المصدر الرسمي لتتبّع التقدّم**، مهام مرقّمة (N.X / N.X.Y) لكل مرحلة، حدّثه بعد كل مهمة تخلص

**اتبع ترتيب المراحل بـ `docs/plans/TASKS.md` بالضبط — لا تقفز لمرحلة قبل ما تخلص وتختبر اللي قبلها.** كل قرار تقني (نمط DataTable مقابل Modal مقابل صفحة منفصلة، آلية الصلاحيات، سلوك الحذف لكل كيان) محسوم مسبقاً هناك — لا تعيد اتخاذه من الصفر.

## سير عمل Git

- **لا ترفع (commit/push) أي تغيير إلا بعد ما تخلص Phase كاملة** من `docs/plans/TASKS.md` وتنجح خطوة الـ "TESTING" الموثّقة بآخر كل Phase بذلك الملف. لا commits جزئية بمنتصف Phase.
- بعد نجاح الاختبار: راجع `git status`/`git diff` كامل قبل `git add`، تأكد ما فيه ملفات حساسة (`.env` وغيره) قبل أي commit.
- رسالة الـ commit تلخّص الـ Phase كاملة (مثال: `"Phase 2: role column + RoleUser-based authorization + policies"`) — مش رسالة لكل مهمة فرعية.
- **لا push لأي remote إلا بطلب صريح من المستخدم بنفس المحادثة** — إنجاز مرحلة لا يعني إذن تلقائي بالـ push.
- بعد كل commit ناجح لمرحلة، حدّث `docs/plans/TASKS.md` (تعليم كل مهام المرحلة `[x]`) كجزء من نفس الـ commit.

## القواعد الحرجة (لا تُخالَف تحت أي ظرف)

هاي القواعد اتفق عليها صاحب المشروع بشكل صريح بعد جلسة تحليل كاملة — أي كود يخالفها يُعتبر خطأ:

1. **وحدة الزيارة = مريض واحد + يوم واحد.** نفس المريض بنفس اليوم = تعديل على الزيارة الموجودة، مش إنشاء زيارة جديدة. حتى لو موظف استقبال مختلف حاول يسجّل، لازم يوجَّه للزيارة الموجودة.
2. **الرصيد الشهري (2 زيارة) على مستوى الموظف ككل** — الموظف + كل تابعيه يستهلكون من نفس الرصيد. مريض تاني (تابع مختلف) بنفس اليوم = زيارة منفصلة تستهلك من نفس الرصيد.
3. **الاستهلاك من الرصيد يحصل مرة وحدة فقط عند إنشاء الزيارة** — إضافة قسم طبي إضافي على زيارة موجودة **لا** تستهلك زيارة إضافية.
4. **الرصيد لا يتراكم شهرياً (No Rollover)** — يُحسب مباشرة (on-the-fly) بعدّ `visits` للموظف وتابعيه ضمن الشهر الحالي. **لا يوجد جدول منفصل للرصيد** — تجنّباً لتكرار مصدر الحقيقة (Single Source of Truth)، `visits` هو المرجع الوحيد.
5. **الخصم ثابت على مستوى القسم الطبي، وليس لكل موظف.** يُنسخ (snapshot) وقت إضافة القسم للزيارة في `visit_departments.applied_discount_percentage` — تغيير الخصم لاحقاً من الإعدادات **لا يؤثر** على زيارات سابقة.
6. **المبالغ المالية اختيارية دائماً** — لا تُشترط لإتمام أي زيارة.
7. **رقم الهوية (9 أرقام) فريد عالمياً** عبر كل الأشخاص بالنظام (موظفين + تابعين)، ويُفحص فوراً عند أي إدخال (من الأدمن أو من الاستبيان).
8. **التابعون (Dependents) كيان موحّد بجدول واحد** (`dependents` مع `type` enum). المريض بالزيارة يُشار له بعمودين على `visits`: `patient_employee_id` و `patient_dependent_id` — بالضبط واحد منهم معبّى، والآخر null دائماً.
9. **الهيكل التنظيمي جدول شجري واحد self-referencing** (`organization_units`)، وليس 3 جداول منفصلة.
10. **دورين فقط منطقياً (`admin`/`receptionist`) — لكن الفحص التقني الفعلي للصلاحيات عبر نظام `RoleUser` الموجود بالكود** (صلاحيات فردية جزئية لكل مستخدم عبر جدول `role_user`، وليس فحص `role === 'admin'` مباشر). عمود `role` هو تصنيف عام + قيمة افتراضية لتعبئة صلاحيات `RoleUser` عند إنشاء مستخدم جديد فقط — قابلة للتعديل يدوياً بعدها لكل مستخدم على حدة. `super_admin` (بوليان منفصل) هو الـ bypass العام الوحيد لكل الصلاحيات. **لا تفترض `Gate::before` يمنح كل الصلاحيات لمجرد `role=admin`** — هذا غير صحيح بعد قرار Phase 2، راجع `docs/plans/phase-2-auth-authorization.md` §2.3.
11. **لا يوجد أي Reporting في هذه المرحلة** — لا تبني شاشات تقارير أو تفترض متطلبات تقارير غير مذكورة صراحة بالوثيقة. البنية يجب أن تسمح بإضافتها لاحقاً، بس مش الآن.
12. **كل عملية جوهرية تُسجَّل بـ `activity_logs`** (إنشاء/تعديل/حذف زيارة، موافقة/رفض استبيان، إضافة موظف) — **عبر `App\Services\ActivityLogService::log(...)` الموجود فعلياً بالكود، وليس جدول/موديل جديد.** جدول `activity_logs` الفعلي بالكود مختلف الشكل عن أي مخطط قديم مذكور بوثيقة تحليل مبكرة — الجدول الحقيقي المعتمد: `user_id, user_name, ip_request, ip_address, event_type, model_name, message, old_data, new_data, created_at` (بدون `updated_at`). لا تنشئ جدول `activity_logs` بديل أو تعدّل مخططه.
13. **حذف السجلات ليس ممنوعاً بشكل عام** — لكل كيان قاعدة حذف محسومة بـ `docs/plans/` تحديداً (مثال: الزيارة قابلة للحذف فعلياً بشرط ملكية/صلاحية، المستخدم له قيود حذف خاصة بمنع حذف الذات وحذف الأدمن من قبل أدمن آخر). **لا تفترض قاعدة حذف عامة واحدة لكل الكيانات** — ارجع لـ `docs/plans/phase-2-auth-authorization.md` و`docs/plans/phase-5-entities.md` لكل كيان على حدة.

## أسلوب العمل المطلوب

- لا تفترض أي متطلب غير موجود بالوثيقة — إذا في غموض أو نقص، اسأل بدل ما تفترض.
- التزم بأسماء الحقول والجداول المذكورة بـ `01-entities.md` حرفياً (snake_case، بالإنجليزية).
- عند كتابة Migrations، حافظ على الـ Unique constraints المذكورة صراحة (خاصة: `national_id` على مستوى النظام، و`(patient_employee_id, visit_date)` و`(patient_dependent_id, visit_date)` على الزيارات).
- الصلاحيات تُبنى عبر Laravel Policies (نمط `ModelPolicy::__call` الموجود) مع فحص فعلي عبر `RoleUser` — راجع القاعدة الحرجة #10 و `docs/plans/phase-2-auth-authorization.md` قبل كتابة أي Policy جديدة، لا تكتفِ بـ `02-roles-permissions.md` وحدها لأنها تصف المصفوفة المنطقية فقط وليس آلية الفحص التقنية المحسومة لاحقاً.
- الـ Workflows بـ `04-workflows.md` هي المرجع لمنطق الـ Controllers/Services — اتبع نفس التسلسل المذكور فيها بالضبط (خاصة تسلسل الفحوصات: تكرار الزيارة → الرصيد → الإنشاء).

## خارج النطاق حالياً

- أي شاشة تقارير (Reporting)
- أي تكامل مالي/محاسبي خارجي
- تفاصيل فرعية داخل القسم الطبي (نوع الدواء/الفحص) — فقط مبلغ إجمالي قبل/بعد لكل قسم